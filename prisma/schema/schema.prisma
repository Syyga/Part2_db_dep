generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
  prismaSchemaFolder = "./models"
}

datasource db {
  provider     = "postgresql"
  url          = env("DATABASE_URL")
}
